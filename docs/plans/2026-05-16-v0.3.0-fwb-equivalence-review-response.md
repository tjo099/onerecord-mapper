# v0.3.0 FWB-Equivalence Plan — Review Response

**Date**: 2026-05-16
**Plan reviewed**: `docs/plans/2026-05-16-v0.3.0-fwb-equivalence.md`
**Reviewers**: four independent external reviewers, parallel, each verifying against the **live** `iata-cargo.github.io/ONE-Record` site + the binding on-disk `3.2-rc2` ontology:
1. Ontology property fidelity · 2. XFWB/OCI/AWB mapping completeness · 3. Plan coherence / reconciliation / recipe / executability · 4. Version-pin soundness / deviation framing / breaking-change risk.

> **For executors:** this document is **mandatory** alongside the plan (same convention as `2026-04-30-v0.3.0-review-response.md`). Where a finding is "apply-during-execution", do it in the named task even though the plan body wasn't rewritten for it.

---

## TL;DR

All four verdicts: **APPROVE-WITH-FIXES**. **Zero blocking correctness defects.** Independent confirmation of the core: Reviewer-1's 25-point citation spot-check against the binding `3.2-rc2` TTL was **all PASS** (schemas spec-faithful); Reviewer-3 confirmed the 5-file recipe matches `src/classes/shipment/*` exactly and found no placeholders; Reviewer-4 ruled the 3.2.0 pin reversal **SOUND** and the reworded deviation #12 a genuine bug-fix; Reviewer-2's FWB-segment matrix shows the Waybill/WaybillLineItem/Shipment/Piece/TransportMovement spine + OCI/eCSD split faithful to the live spec. A bonus cross-check (Reviewer-1) found the **live endorsed `2025-07-standard` ontology also self-labels `owl:versionInfo "3.2-rc2"`** — independently validating Locked Decision 2.

The fixes are completeness/honesty/precision, not correctness reversals. Disposition below.

---

## Findings & disposition

### Fixed inline in the plan (this commit)

| # | Sev | Reviewer(s) | Finding | Fix applied |
|---|---|---|---|---|
| F1 | HIGH (struct.) | R3 #1 | "Batch 4 may run standalone" is false — `regulated-entity/`/`security-declaration/` dirs don't exist; standalone yields a class with only `schema.ts` → fails `class-completeness`. | Phase 2b preamble rewritten: Batch 4 layers on existing Tasks 1.2/1.3/1.4/2.1/2.5/2.6; no standalone path; if Phase 1/2 unrun, Batch 4 must run the full recipe first. |
| F2 | HIGH | R2 #1 | `CustomsInformation` omits `issuedForPiece`/`issuedForShipment` → orphan OCI LO, FWB OCI block unreconstructable. | Both props added to `CustomsInformationSchema` (Task 1b.4) + Step-1 test directive. |
| F3 | HIGH | R3 #2 | Supersession map incomplete (omits original Task 1.1 ScreeningExemption, 2.3/2.4/2.5/2.8, 1.5/2.9/11.1/11.2/11.4). | Reconciliation map expanded with 4 new rows + explicit "skip as written" list. |
| F4 | HIGH | R3 #3, R4 #2 | `CARGO_ONTOLOGY_VERSION` flip blast radius under-specified; public-API behaviour break not in BREAKING block. | Task 2b.4 Step 3 enumerates constants.test/server-information factory/snapshots/peer-compat decision; Step 5 BREAKING block + MIGRATING expanded. |
| F5 | MED | R1 #1, R2 #4 | `PartySchema` omits real `:Party.otherIdentifiers`. | Added to `PartySchema` (Task 1b.9) + test directive. |
| F6 | MED | R2 #2 | `Waybill` omits `otherChargesIndicator` (OTH waybill-level PP/CC) + `taxAmount`. | Both added to `WaybillSchema` (Task 1b.8). |
| F7 | MED | R1 #2 | Plan's Task 1b.0 Step 2 told implementer to confirm `:CurrencyValue rdfs:subClassOf :Value` — false (it declares its own `numericalValue`). Schema body was already correct. | Step 2 reworded: `:CurrencyValue` is NOT a `:Value` subclass; grep-by-term not by line. |
| F8 | MED | R2 #3, R4 #3 | Deviation #12 framed `Shipment.securityDeclarations` as generic forward-compat; the property is **Piece-domain** (`owl:comment "Domain :Piece"`). | #12 reworded: Piece-domain stated; non-conformant for two cited reasons; the original plan's backwards inverse-traversal claim explicitly called out as fixed. |
| F9 | MED | R3 #4 | Phase 4 (cross-node) fixtures use Path-A-removed keys; "zero file overlap" claimed only vs Phase 3. | Reconciliation map: Phase 4 row added (consume spec keys, rebuild fixtures after Phase 1b, sequence after). |
| F10 | MED | R3 #5 | Stale `domain-constraints`/conformance rows assert removed `shipmentInformation`/`pieceCount`. | Task 2b.4 Step 4 extended: audit + correct `src/dispatch/domain-constraints.ts` and stale matrix rows. |
| F11 | MED | R4 #3 (open-flag gap) | README marketing claim not reconciled, only the matrix (review-response line 232 precedent). | Task 2b.4 Step 5 requires README positioning change; added Open flag 5. |
| F12 | MED | R4 #1, R3 #1 | Hardcoded class-count literals (37/38/39) wrong if existing Phase 1/2 already registered. | Reconciliation map note + Task 1b.10 Step 4 rewritten to "auto-derived, never assert a literal". |
| F13 | LOW | R2 #6 | `BillingDetails` deferred silently (violates the plan's own deviation discipline). | Deviation #16 added (scoped-out FWB gap, named follow-up). |
| F14 | LOW | R1 #4, R4 #4 | `waybillPrefix`/`waybillNumber` pattern presented as ontology-faceted; it's editorial. | Folded into Task 2b.4 Step 5 MIGRATING (alphanumeric invariant note); Open flag 2 already disclosed. |

### Apply during execution (binding directives — plan body not rewritten)

- **D1 (R3 #7) — snapshot filename:** the harness writes `<className.toLowerCase()>.snapshot.json` with **no dashes** (`test/snapshots/classes/_harness.ts:26`, `common.ts:38`). For multi-word classes the file is `waybilllineitem.snapshot.json`, `lineitempackage.snapshot.json`, `customsinformation.snapshot.json`, `co2emissions.snapshot.json`, `otheridentifier`-N/A, `regulatedentity.snapshot.json`, `securitydeclaration.snapshot.json`. **Every `git add … .snapshot.json` must use the harness-generated no-dash filename, not `<dirname>.snapshot.json`.** Prefer `git add test/snapshots/classes/` (directory) over naming the file.
- **D2 (R3 #6) — Company PBT:** Task 1b.1 omits the PBT step (asymmetry vs 1b.2/1b.3/1b.5/1b.11/1b.12). Either add `test/property/company-roundtrip.property.test.ts` per the TEMPLATE for consistency (preferred), or accept unit-harness-only coverage (conformance §7.1 allows it). Decision: **add it** for consistency.
- **D3 (R1 #3, R2 #7) — ULD inherited props** *(SUPERSEDED by R3-3 — now promoted into the plan body as **Task 1b.11 Step 1b**; this directive is retained as history only)*: `tareWeight`/`inUnitComposition`/`remarks`/`manufacturer` are NOT in the `:ULD` restriction block — they apply via `:LoadingUnit`/`:PhysicalLogisticsObject` superclass / `owl:comment` domain. Task 1b.11 **Step 1b** `grep`-verifies each; drop any not inherited; annotate `// domain-inherited via :LoadingUnit / :PhysicalLogisticsObject — NOT a :ULD class restriction`. (Round-3 reviewer separately verified all four ARE genuinely inherited — none dropped.)
- **D4 (R1 #5, R2 #5) — citation drift:** Reviewer-1's 25-point spot-check found the plan's `3.2-rc2` line cites **accurate**; Reviewer-2 reported ~3-8 line drift. They examined the same file; the discrepancy is unresolved. **Mitigation (do this regardless):** every "verify against the TTL" step greps by the `:term` / `### …#Term` anchor, NOT by the cited line number. Line numbers in this plan are advisory; the term + range + cardinality assertions are authoritative (and were independently PASS-verified by Reviewer-1 and the original 4 verification agents). Do not bulk-rewrite the cites (churn risks introducing errors Reviewer-1 proved aren't there).
- **D5 (R1 #6) — RegulatedEntity restriction sub-lines:** the per-restriction `allValuesFrom`/`maxCardinality` blocks are split; the plan's paired ranges (e.g. "9069-9070") are approximate. Schema is correct. Confirm via grep at execution; no schema change.
- **D6 (R1 #5) — Value/Dimensions ontologically unbounded:** `Shipment.totalDimensions` and `TransportMovement.co2Emissions` have no `owl:maxCardinality` in the ontology; the plan models them single (`.optional()`). Pragmatic FWB reading — keep, but note in the conformance matrix that they are ontologically unbounded, modelled single.

### Accepted as-is (no change — reviewer concurrence)

- Pin reversal (3.2.0, `3.2-rc2` binding; master = forward-ref): **SOUND** (R4 — live releases page + on-disk versionInfo + compliance §1 all concur; reversal of review-response finding 1.6 justified & recorded).
- DD-1 / deviation #14 (defer legacy `{unit,value}` retrofit): **defensible** (R4 — compliance §4 line 203 rates it MODERATE, Verdict line 222 schedules it separately; documented split, owner flag 1).
- 5-file recipe TEMPLATE ≡ `src/classes/shipment/*`: **exact match, no defects** (R3 recipe-diff). Harness/factory/PBT wrapper signatures verified against real `_harness.ts`/`_helpers.ts`/`common.ts`.
- Placeholder scan: **PASS** (R3 — Task 1b.0 Step 2 is an acceptably-concrete TDD verify step, not a placeholder).
- File-disjointness from blocked Phase 3: **confirmed** (R3 — Phase 3 touches `booking-option/`/`booking-flow/`/booking enums; Phase 1b is disjoint; `src/codes/enums.ts` co-edited but on disjoint enums, append-only).
- FWB-segment coverage (Reviewer-2 matrix): every segment lands correctly except the F2/F6 gaps now fixed; PPD/CCD scope-guard correctly honored; eCSD→SecurityDeclaration split correct; SecurityDeclaration 12-prop set exactly matches 3.2.0.

---

---

## Second review round — owner's external reviewer (2026-05-16)

**Correction of the round-1 TL;DR:** the claim "zero blocking correctness defects" was **overstated**. The round-1 inline fix F2 (adding `CustomsInformation.issuedForPiece`/`issuedForShipment`) itself introduced a **cardinality defect** — it modelled `issuedForPiece` as `z.array(safeIri())`, but the pinned ontology has `owl:maxCardinality "1"` on BOTH (`f:\dev\cargo-erp\tmp\onerecord-3.2.0\IATA-1R-DM-Ontology.ttl:6274-6277` & `:6278-6281`, verified). A hand-applied review fix was not re-verified to the same file:line standard as the original agent pass — exactly the failure mode this project's "verify, don't infer" rule exists to catch. The reviewer was correct; do not start execution on round-1 state.

| # | Sev | Finding | Verified? | Disposition |
|---|---|---|---|---|
| R2-1 | HIGH | Batch 4 not executable — 2b.2/2b.3 "Modify" non-existent dirs; handoff runs 2b.1→2b.4 directly | Confirmed (dirs absent) | **Fixed:** new **Task 2b.0** creates both classes' full 5-file recipe + registration + factory/unit/snapshot/PBT (idempotent); 2b.2/2b.3 become verify-or-correct. |
| R2-2 | HIGH | `CustomsInformation.issuedForPiece` modelled array; ontology `maxCardinality 1` @6277 | Confirmed (TTL 6274-6277 & 6278-6281 both max 1) | **Fixed:** both → `safeIri().optional()`; header/note corrected; negative-test (reject array) mandated. Round-1 overstatement retracted. |
| R2-3 | MED | `src/dispatch/field-types.ts` missed — `FIELD_TYPES` still maps removed keys | Confirmed (`field-types.ts:63-64,69-71,88`) | **Fixed:** Task 2b.4 Step 4b — rewrite `field-types.ts` (remove 6 stale, add ~40 new mappings incl. Organization→Company), update its tests + graph-walk comments. |
| R2-4 | MED | Goal "fully and correctly model … reconstruct" contradicts deviation #16 (BillingDetails) | Confirmed (plan Goal vs #16) | **Fixed:** Goal downgraded to owner's exact wording ("3.2.0-conformant for the covered class set, with documented deviations and 3.2.1-forward extensions"); #16 stands as named follow-up. |
| R2-5 | MED | Shipment failing-test fixture `textualHandlingInstructions:'Keep dry'` (string) vs schema `z.array` | Confirmed (plan :503 vs :536; ontology supports array) | **Fixed:** test fixture → `['Keep dry']`. |
| R2-6 | LOW/MED | D1–D6 should be promoted into task bodies (subagent-per-task risk); D1 snapshot-name, D2 Company PBT | Valid | **Fixed:** D1 promoted into the recipe TEMPLATE (snapshot-filename gotcha box); D2 promoted into Task 1b.1 (explicit Company PBT Step 4b). D3–D6 remain binding-via-this-doc (verification-style, lower miss-risk) — the plan header mandates this doc. **(Superseded by R3-3: D3 was subsequently promoted into the plan body — Task 1b.11 Step 1b; only D4–D6 remain doc-only.)** |

**Owner flag decisions (resolved this round)** — recorded in the plan's "Owner decisions — RESOLVED" section: (1) keep DD-1 defer, don't market as "strict 3.2.0"; (2) relax with corrected `waybillPrefix` wording ("maxLength 3 + editorial alnum policy"); (3) ship deprecated `accountingInformation` (#15); (4) `Shipment.securityDeclarations` forward-compat only, absent from factory/snapshot; (5) README+Goal exact wording applied; (6) ontology-version = exact `'3.2'`≡`'3.2.0'` legacy alias, reject `'3.2.1'`, NOT `startsWith`.

---

## Third review round — owner's external reviewer (2026-05-16)

**Pattern, stated honestly:** round 2's fix R2-1 (adding Task 2b.0) *itself* introduced an ordering contradiction (2b.0 depends on 2b.1's enum but is numbered/sequenced before it; the handoff omitted 2b.0). This is the **second** consecutive round where a hand-applied fix introduced a new defect (round 2: F2→issuedForPiece array; round 3: R2-1→2b.0 ordering). The recurring lesson: an edit that adds a task/field needs the same downstream-consistency sweep (ordering, handoff, dependent steps, summary tables) that a fresh authoring pass gets. All six round-3 findings verified against the current plan text and fixed.

| # | Sev | Finding | Verified? | Disposition |
|---|---|---|---|---|
| R3-1 | HIGH | Batch 4 order contradictory — 2b.0 before 2b.1 but depends on it; handoff `…→2b.1→2b.4` omits 2b.0 | Confirmed (plan :887 vs :1112) | **Fixed:** explicit order banner (`2b.1→2b.0→2b.2→2b.3→2b.4`) in Phase 2b + before Task 2b.0; handoff rewritten with the full ordered sequence; 2b.2/2b.3 Step 2 made conditional (PASS-verify if 2b.0 ran, FAIL-correct on the alternate path). |
| R3-2 | MED | Organization→Company sweep misses uppercase (`testIri('Organization',…)`) + `field-types.ts:28 AccountNumber.issuedBy:'Organization'` | Confirmed (`grep "organization"` ≠ case-covering; `field-types.ts:28`) | **Fixed:** Task 1b.10 Step 2 → `rg -n "Organization\|organization"` across src/test/docs/snapshots with explicit hit checklist. ~~Task 2b.4 Step 4b explicitly repoints `AccountNumber.issuedBy`→`'Company'`.~~ **(SUPERSEDED by R4-1: that repoint was a fabricated-ontology fact; Step 4b now DELETEs `AccountNumber.issuedBy` — no 3.2.0 basis.)** |
| R3-3 | MED | D3 (ULD inherited-prop verification) left only in companion doc | Confirmed (1b.11 had no grep/annotate step) | **Fixed:** promoted into Task **1b.11 Step 1b** — grep-verify each inherited field vs `:LoadingUnit`/`:PhysicalLogisticsObject`, drop if not inherited, annotate. |
| R3-4 | MED | "Step 2–6" shorthand contradicts Self-Review "no placeholders" claim | Confirmed (1b.2/1b.3/1b.4/1b.5/1b.12) | **Fixed (honesty, not expansion):** Self-Review §2 rewritten — no longer claims uniform expansion; states the compression is template-backed, names which tasks use it, and requires the subagent to expand against the TEMPLATE + inline specifics. (Full expansion of ~5 tasks rejected as churn-prone; the honest scoping is the proportionate fix.) |
| R3-5 | LOW | Owner `waybillPrefix` wording not propagated (file-structure :85 "spec patterns"; MIGRATING :1077 "per 3.2-rc2") | Confirmed | **Fixed:** both reworded — `waybillNumber` `[A-Z0-9]+` per ontology; `waybillPrefix` maxLength-3 + **editorial** alnum policy, explicitly NOT a spec pattern (closes the reintroduced F14). |
| R3-6 | LOW | Company PBT `subOrganization` uses `iriArb('person')` | Confirmed (Step 4b) | **Fixed:** `parentOrganization`/`subOrganization`→`iriArb('company')`, `basedAtLocation`→`iriArb('location')`, `contactPersons`→`iriArb('person')`. |

---

## Fourth review round — 3-reviewer confirmation pass (2026-05-16)

**The confirmation pass did its job and the pattern recurred a third time.** Independent reviewers confirmed the *substantive* core is now solid: the round-3 ordering fix (R3-1) is globally swept (Reviewer-A order-consistency matrix all-Y, isolated-subagent dry-run of `2b.1→2b.0→2b.2→2b.3→2b.4` clean); R2-2 `issuedForPiece`/`issuedForShipment` cardinality is correct, **no array survivor, no round-1 regression** (Reviewer-C, re-verified vs TTL 6274-6281); all four ULD inherited props confirmed genuinely inherited via `:LoadingUnit`/`:PhysicalLogisticsObject` (none dropped); Company PBT arbitraries schema/ontology-sound; the Organization→Company sweep and waybillPrefix wording are fully consistent. **But** the round-3 fixes themselves left/added 4 issues:

| # | Sev | Finding | Verified? | Disposition |
|---|---|---|---|---|
| R4-1 | LOW–MED | `'AccountNumber.issuedBy'` repoint to `'Company'` is groundless — `:issuedBy` (TTL 1407-1411) is `rdfs:range :Person`, `Domain :SecurityDeclaration`; **not** on `:AccountNumber` (4778-4797). My R3-2 text "Company satisfies the `:issuedBy` `:Organization` range" was a **fabricated ontology fact** (doubly false: range is Person, prop not on AccountNumber). | Confirmed against TTL | **Fixed:** Step 4b now says **DELETE** `'AccountNumber.issuedBy'` (no 3.2.0 basis); the false justification removed. |
| R4-2 | MED | Step 4b gave a subtype-substitutability rationale for one Organization-range pin but not the other three (`Insurance.coveringOrganization`/`RegulatedEntity.owningOrganization`/`Company.parent/subOrganization`) — internally inconsistent reasoning (Reviewer-A C1). Values are ontology-sound (Reviewer-C); the *rationale* was presented inconsistently. | Confirmed | **Fixed:** single uniform governing rationale stated once in Step 4b for all Organization-range→Company pins. |
| R4-3 | MED | Self-Review §2 (the R3-4 "honesty" fix) **misnamed its own compressed-task set** — claimed 1b.2/1b.3/1b.4/1b.5/1b.12 compress; only 1b.3/1b.4 actually do (Reviewer-B). The honesty fix was itself inaccurate — the meta-pattern, a third time. | Confirmed (task bodies inspected) | **Fixed:** §2 corrected to exactly {1b.3, 1b.4}; 1b.2/1b.5/1b.12 moved to the expanded set. |
| R4-4 | MED | Task 2b.0 Step 4 `public-exports` snapshot-diff expectation was "RE/SD-only added", not path-conditional — on the alternate path (Phase 1/2 pre-registered) the diff is "changed" and the strict-reject gate misleads (Reviewer-A C2). | Confirmed | **Fixed:** 2b.0 Step 4 snapshot expectation made path-conditional (mirrors the 2b.2/2b.3 Step 2 conditional). |
| R4-5/6 | LOW | Self-Review §1 omitted 2b.0; 2b.0 Step 1 didn't point to where the verbatim schemas live; companion-doc D3/R2-6 stale "Step 1"/"D3 doc-only" history refs. | Confirmed | **Fixed:** §1 lists `2b.1→2b.0→2b.2→2b.3→2b.4`; 2b.0 Step 1 points to 2b.2/2b.3 Step 3 blocks; D3 + R2-6 cells annotated SUPERSEDED-by-R3-3. |

## Net — honest convergence assessment

Four rounds. Severity is **monotonically decreasing**: round 1 (structural/spec) → round 2 (HIGH executability + a HIGH ontology defect) → round 3 (1 HIGH ordering + MED sweeps) → round 4 (0 HIGH; MED doc-coherence + 1 LOW-MED groundless-mapping). The **substantive correctness is now independently confirmed solid**: schemas spec-faithful (round-1 25-point + round-4 re-spotchecks), ontology cardinality correct, Phase 2b ordering globally consistent and dry-run-clean, executability self-contained, no round-1 regression. The residual risk is **documentation coherence**, not correctness or executability.

**The honest meta-finding:** consecutive rounds, a hand-patch carried a small defect (R2: arrayed a maxCard-1 prop; R3: ordering contradiction; R4: fabricated ontology claim + mis-enumerated its own honesty fix; R5: F12 "don't hardcode count" fix skipped 1b.11/1b.12). Logged in project memory. Diminishing returns to keep hand-patching — each round finds smaller, non-correctness nits.

---

## Fifth review round — 3-reviewer confirmation pass (2026-05-16)

Tightly scoped to round-4 deltas + a holistic executor dry-run. Outcome — the gradient bottomed out:

| Reviewer | Verdict | Result |
|---|---|---|
| Ontology-fidelity | **CONFIRMED-CLEAN** | Every round-4 ontology claim VERIFIED against the TTL with file:line (the R4-1 `:issuedBy`→`:Person` correction, the 4 Organization-range claims, `:Company ⊑ :Organization` 6113-6114, R2-2 6274-6281). **Zero fabricated/incorrect assertions introduced.** No regression. |
| Delta-consistency + regression | APPROVE-WITH-FIXES (1 LOW) | All R4-1…R4-6 internally CONSISTENT; Self-Review §2 enumeration re-verified task-by-task (exactly {1b.3,1b.4} compress — accurate); R2-2/R3-1/R3-2/R3-5 not regressed. Only residual: 1 LOW audit-log nit. |
| Holistic executor dry-run | APPROVE-WITH-FIXES (1 MED, 1 LOW) | Full 19-task subagent dry-run: **every task HAS-WHAT-IT-NEEDS.** One MED: F12's "no hardcoded count" fix skipped Task 1b.11/1b.12 Step 5 ("→38"/"→39") — the F12-class defect in the two tasks the fix's scope missed. |

| # | Sev | Finding | Disposition |
|---|---|---|---|
| R5-1 | MED | 1b.11 Step 5 "count → 38" / 1b.12 Step 5 "→ 39" contradict F12's "never assert a literal"; a subagent doesn't see the reconciliation-map exemption | **Fixed:** both de-literal'd to "auto-derived, confirm PASS, do not assert a literal (same rule as 1b.10 Step 4, F12)". |
| R5-2 | LOW | review-response R3-2 cell still says "repoints AccountNumber.issuedBy→Company" (R4-1 retracted to DELETE) — D3/R2-6 got SUPERSEDED notes, R3-2 didn't | **Fixed:** R3-2 cell struck-through + "SUPERSEDED by R4-1" annotation (audit-log hygiene; plan body was already uniformly DELETE). |
| R5-3 | LOW (optional) | `'Company.contactPersons':'Person'` is an `:Actor`→`:Person` contract narrowing; not a fabrication, but worth a mirroring note (Reviewer-2, explicitly non-blocking) | **Fixed (zero-risk):** one clarifying sentence appended to the uniform-rationale paragraph. |

---

## Sixth review round — owner's reviewer (2026-05-16)

A different failure mode than rounds 2-5 (which were fixes introducing defects): round 6 caught a **Path-A scope omission** — when `Party.accountNumbers` (Task 1b.9) pulled `AccountNumber` into the FWB SHP/AGT coverage path, the "apply Path-A spec-correctness to every FWB-spine class" logic was not swept onto `AccountNumber` itself, which still shipped v0.2 non-spec keys.

| # | Sev | Finding | Verified? | Disposition |
|---|---|---|---|---|
| R6-1 | MED | `account-number/schema.ts:8-10` ships non-spec `accountNumber`/`issuedBy`/`accountType`; 3.2.0 `:AccountNumber` (TTL 4778-4797) = `accountNumberType`+`textualValue` only; `:issuedBy` (1407-1411) is a `:SecurityDeclaration`/`:Person` prop. AccountNumber is in the FWB path via `Party.accountNumbers` + the SHP/AGT account-coverage claim. | Confirmed (schema + TTL read) | **Fixed:** new **Task 1b.9b** — Path-A AccountNumber rewrite (schema→`accountNumberType`+`textualValue`, verify `:AccountType` in DM TTL, factory/snapshot/unit/PBT, conformance row, CHANGELOG BREAKING). Spec-correction + File-Structure tables updated. (The `FIELD_TYPES['AccountNumber.issuedBy']` DELETE was already correct in 2b.4 Step 4b; post-fix AccountNumber has no LO-ref props.) |
| R6-2 | LOW | Spec-correction table row 56 said "Ship Piece only; defer Shipment" — contradicts the RESOLVED owner decision (Shipment.securityDeclarations = optional forward-compat, absent factory/snapshot, 3.2.1-forward; not "deferred"). Task body was correct. | Confirmed | **Fixed:** row 56 reworded to match the resolved decision + deviation #12. |
| R6-3 | LOW | cargo-erp compliance report §6 callout + footer still said "binding 3.2.1 ontology TTL"; my §0/§6-tally said "4-reviewer-approved" (now 5+ rounds). | Confirmed | **Fixed:** §6 callout + footer carry a "provenance corrected — pin is 3.2.0 / `3.2-rc2`, 3.2.1 is unreleased master cross-ref" note; tally → "externally reviewed, 5 rounds". |

## Net — convergence reached (updated through round 6)

Six rounds. The fix-induced-defect gradient (rounds 2-5) did bottom out — round 5's two independent reviewers (ontology CONFIRMED-CLEAN; holistic 19-task dry-run all-green) confirmed the *changed* content is clean. **But round 6 honestly resets the "done" claim:** it found a genuine **MED scope omission** (`AccountNumber` shipped v0.2 non-spec keys and was never added to the Path-A class set, even though `Party.accountNumbers` pulls it into FWB SHP/AGT coverage). That is not a doc-hedge — it is a real, additive correction (now Task 1b.9b). The lesson: the rounds 2-5 reviewers swept *changed regions*; they did not ask "is the Path-A class set complete" — a different angle, and it surfaced something real.

**Honest assessment, no overclaim:** every finding to date is verified-then-fixed; severity has stayed contained (no HIGH since round 3; round 6's MED is a clean new task, not a correctness error in already-planned schemas); the substantive content of what is planned is independently verified against the binding `3.2-rc2` ontology. **However**, six consecutive rounds have each surfaced ≥1 real item, and round 6 proved a fresh review *angle* can still find a genuine MED — so I will **not** assert "the floor, further review unproductive." The realistic expectation: another round from a new angle may find another contained item; the curve is decaying but not provably at zero. This is now a judgement call between (a) diminishing returns vs (b) the residual that a 7th angle finds one more contained gap — explicitly the owner's risk decision for a breaking-change release, not something I should declare closed. The plan is **execution-ready in content**; whether the review loop continues is the owner's call.
