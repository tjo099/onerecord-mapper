# Spec deviations

This document tracks places where `@flaks/onerecord` v0.1.x deliberately
diverges from canonical IATA OneRecord behavior. Read this before adopting
if your integration must match the cargo data model 3.2 / API spec 2.2.0
verbatim.

For maintainer-side reasoning (why each deviation exists), see the "Plan
deviations" section of [`CHANGELOG.md`](../CHANGELOG.md).

## 1. Booking — `acceptBookingOption` skips an intermediate state

**Spec §5.4 STATE_DIAGRAM**: `BookingOption.accept` transitions through
a `BookingOptionRequest` intermediate state before producing a `Booking`.

**v0.1.x behavior**: the typed transition wrapper
`acceptBookingOption(opt)` returns a `Booking` directly, skipping the
`BookingOptionRequest` intermediate. This matches **spec §5.2 signature**
but contradicts the §5.4 state diagram.

**Resolution path**: v0.2.0 will either implement the intermediate state
or formalize the shortcut via an IATA spec amendment PR.

**What to do today**: if your integration depends on the intermediate
state being persisted, do **not** rely on the `acceptBookingOption`
wrapper in v0.1.x. Drive the transition manually via `applyChange` on
the `BookingOption` codec, persisting the intermediate state explicitly.

## 2. Graph-walk validation kinds reserved but not emitted

**Spec**: a graph-walk validation pass would emit
`duplicate_id_in_graph`, `missing_id`, `wrong_type_for_endpoint`, and
`missing_type` for cross-node integrity violations.

**v0.1.x behavior**: per-class deserializers do not run a graph-walk.
Zod fires first for shape errors, so these `kind` values exist in the
`ParseError` discriminated union but are not emitted by per-class
deserializers in practice. The discriminated-union exhaustiveness still
works at the type level — TypeScript requires you to handle them — they
just don't fire at runtime in v0.1.x.

**Resolution path**: v0.2.0 plans a higher-level dispatch layer that
runs graph-walk validation and emits these kinds for genuinely
cross-node violations.

**What to do today**: write your `ParseError` exhaustive switch as if
all 22 kinds are reachable (TypeScript will require this anyway). When
the dispatch layer ships in v0.2.0, your code will already handle the
new emission paths.

## 3. Empty-array rejection happens pre-Zod, not post-Zod

**Spec §7.1**: cardinality violations (including `[]` on a field that
requires omission) should produce a validation error of kind
`cardinality_violation`.

**v0.1.x behavior**: `findFirstEmptyArray` runs in `preValidate`
**before** Zod schema evaluation, producing `cardinality_violation` for
empty arrays at any depth. The spec doesn't dictate when in the
validation pipeline this check runs, only that it must produce a
cardinality error — the observable outcome is identical.

**Why pre-Zod, not post-Zod**: post-Zod placement is unreachable for
fields whose schema is `safeIri().optional()` — Zod emits
`zod_validation` first for the wrong-shape case. Pre-Zod placement
matches the spec's "reject empty arrays on the wire" intent regardless
of the field's declared schema type.

**What to do today**: nothing. The error class on the wire is the
spec-mandated `cardinality_violation`. This deviation is invisible to
consumers.

## 4. FSU code list is hand-pinned, not xlsx-derived

**Spec**: the 26-code FSU event-code list in
`test/fixtures/iata/xfsu-status-codes.json` should be derived from
the IATA Cargo XFSU-Status-Codes.xlsx working draft.

**v0.1.x behavior**: the 26 codes are hand-pinned in the fixture
rather than generated by `scripts/convert-iata-xlsx.ts`. The codes
match the working draft as of the v2 implementation plan authoring
date. Field `pinnedSha` is set to the placeholder
`manually-pinned-v0.1.0`.

**Resolution path**: v0.2.0 replaces the placeholder SHA with a real
upstream xlsx SHA via the convert script and the
`scripts/check-fixture-drift.ts` job in nightly CI.

**What to do today**: nothing — the codes are correct. The fixture
metadata only matters if you're auditing provenance.

## 5. Circular-reference detection is best-effort

**Spec**: `circular_reference` is a documented `ParseError.kind` for
self-referential graphs.

**v0.1.x behavior**: a simple self-reference (e.g. `wb.self = wb`)
triggers `depth_limit_exceeded` first because `preValidate`'s
depth/node walk recurses into the cycle before the dedicated
`detectCycle` step runs. Diamond patterns (shared references that are
not cycles) are correctly accepted without false positives.

**What to do today**: handle both `depth_limit_exceeded` and
`circular_reference` in your error switch when receiving graphs from
untrusted sources. They are semantically equivalent — both indicate a
malformed graph that the library refused to parse.

---

## How deviations are tracked

- **Source of truth for deviations**: this document.
- **Source of truth for reasoning**: `CHANGELOG.md` "Plan deviations".
- **Source of truth for spec semantics**: the upstream
  [IATA-Cargo/ONE-Record](https://github.com/IATA-Cargo/ONE-Record)
  repository.

If you find a deviation not listed here, please open a bug report — it's
either a genuine bug or a missing entry in this document.
