import { describe, it } from 'vitest'

describe('deserialize -> duplicate_id_in_graph', () => {
  it.skip('placeholder: duplicate_id_in_graph is emitted by graph-walk validation (Phase 15+ facade)', () => {
    // The kind exists for graph-walk validation. v0.1.0 per-class deserializers
    // don't traverse cross-object graphs.
  })
})
