import { describe, it } from 'vitest'

describe('deserialize -> forbidden_state_transition', () => {
  it.skip('placeholder for Phase 10 — Booking state transitions (T55-T60)', () => {
    // Filled in when canTransition + STATE_DIAGRAM + applyOperationWithStateCheck land.
  })
})
