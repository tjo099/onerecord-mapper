import { describe, it } from 'vitest'

describe('deserialize -> missing_id', () => {
  it.skip('placeholder: missing_id surfaces as zod_validation in v0.1.0 (kind reserved for graph-walk validation)', () => {
    // Reserved for graph-walk validation that distinguishes "no @id" from "wrong @id format".
  })
})
